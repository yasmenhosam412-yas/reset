name: new_project
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  json_annotation: ^4.11.0
  supabase_flutter: ^2.12.4
  fpdart: ^1.2.0
  get_it: ^9.2.1
  injectable: ^3.0.0
  flutter_bloc: ^9.1.1
  go_router: ^17.2.2
  meta: ^1.17.0
  bloc: ^9.2.0
  shared_preferences: ^2.5.5
  url_launcher: ^6.3.2
  firebase_core: ^4.7.0
  firebase_messaging: ^16.2.0
  flutter_local_notifications: ^21.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  json_serializable: ^6.13.1
  # Run via project Dart to avoid global flutterfire kernel / pub advisories issues:
  #   dart run flutterfire_cli:flutterfire configure
  flutterfire_cli: ^1.3.2


flutter:
  uses-material-design: true
